#include<iostream>
#include<string>
#include<vector>
#include<algorithm>

using namespace std;

vector<string> ans;
void gen(vector<string> &sl, vector<string> &a, int k) {
	if (k == 6) {

	}
	else {

	}
}

int main() {
	vector<string> slovs(6);
	for (int i = 0; i < 6; i++) {
		cin >> slovs[i];
	}
	do {
		vector<string> ans1(slovs[2].size());
		gen();
	} while (next_permutation(slovs.begin(), slovs.end()));
	

}