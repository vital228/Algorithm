#include<stack>
#include<queue>
#include<deque>
#include<map>
#include<set>
#include<string>
#include<numeric>
#include<iostream>

using namespace std;

int main() {
	map<string, int> m;
	set<int> s;
	/*for (set <int>::iterator it = s.begin(); it != s.end(); ++it) {

	}
	for (auto it = s.begin(); it != s.end(); ++it) {

	}*/
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		string p;
		cin >> p;
		m[p]=
	}
	cout << m["a"];
}